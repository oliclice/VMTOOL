要求任何config配置更新后都要即时生效
- 例如，用户在GUI界面修改了默认导出路径,那么相关界面也要同步更新

要求对所有py文件使用Python内置的 cProfile 模块可统计各函数执行耗时，快速定位“长耗时”函数
把耗时任务移出主线程，不让它阻塞事件循环
永远不要在非GUI线程（子线程）中直接操作任何界面元素。正确的做法是，在子线程中定义并发射（emit）信号（Signal），主线程通过连接（connect）这个信号的槽（Slot）来安全地更新UI